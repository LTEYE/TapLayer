[project]
name = "taplayer"
version = "1.1.0"
description = "TapLayer (击层) — turn one key into many shortcuts with tap layers."
requires-python = ">=3.10"
license = { text = "GPL-3.0-or-later" }
dependencies = [
    "PySide6>=6.5,<6.13"
]
